<div class="container">
  <% if @tweet.errors.any? %>
    <p style="color:red"><%= pluralize(@tweet.errors.count, "error") %> prohibited this post from being saved</p>
  <% end %>
  <h1>Create a new tweet</h1>
    <div id="form">
      <%= form_for @tweet do |f| %>
      <%= f.label :content %> <br>
      <%= f.text_area :content, class: "form-control" %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
